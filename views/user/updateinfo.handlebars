<link rel="stylesheet" href="/css/cstyle.css">
<div class="row">
	<div class="col-md-6 mx-auto">
		<div class="card card-body">
			<h3 class="text-center">Update Info</h3>
			<form action="../cuser/saveeditedcregister/{{user.id}}?_method=PUT" method="post">
				<input type="file" id="posterUpload" name="posterUpload" accept="image/png,image/jpeg,image/jpg,image/gif"> 

<img id="poster" src="/img/profile.png" alt="" class="border img-thumbnail img-fluid"
                        style="height:95%;width:100%">
    {{!—- Hidden field to submit URL of poster to the server --}}
<input id="posterURL" type="hidden" name="posterURL">
{{!-- Practical 10 Exercise 01 --}}
<div class="font-weight-bold text-danger small" id="posterErr" style="display:none"></div>
			


				<div class="form-group">
				<label for="gender">Gender</label>
				<div class="form-check form-check-inline">
					
							<input class="form-check-input" type="radio" id="Male" value="Male" name="gender" {{radioCheck user.gender 'Male'}}>
							<label class="form-check-label" for="Male">Male</label>
						</div>
						<div class="form-check form-check-inline">
							<input class="form-check-input" type="radio" id="Female" value="Female" name="gender" {{radioCheck user.gender 'Female'}}>
							<label class="form-check-label" for="Female">Female</label>
						</div>
						<div class="form-check form-check-inline">
							<input class="form-check-input" type="radio" id="None" value="Prefer Not to say" name="gender" {{radioCheck user.gender 'Prefer Not to say'}}>
							<label class="form-check-label" for="None">Prefer not to say</label>
						</div>
				</div>
				
				<div class="form-group">
					<label for="contact">Contact Number</label>
					<input type="tel" name="contact" pattern=".{8}"class="clogininputBox" value="{{user.contact}}" required>
				</div>
				<div class="form-group">
					<label for="address">Address</label>
					<input type="text" name="address" class="clogininputBox" value="{{user.address}}" required>
				</div>
					<div class="form-group">
					<label for="address">Postal Code</label>
					<input type="text" name="postalcode" pattern=".{6}" class="clogininputBox" value="{{user.postalcode}}" required>
				</div>
				<button type="submit" class="addfeedback">Update</button>
				
			</form>
		</div>
	</div>
</div>